package Day2;

public class ClassNotes {

    /*
    *
    * What is an Array?
    * - Array allows you to store multiple values/sets of data into a single variable (place holder)
    * -Array is defined by symbol []
    * -There are string array and int array
    * -Array always starts from 0 instead of 1
    * -There are two type of array which is Linear and dynamic array
    *
    *
    * Linear array is written in a single line fpr instance,
    *
    * */
}
