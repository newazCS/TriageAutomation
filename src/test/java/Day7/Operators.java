package Day7;

/*
     xPath("//*[@attribute='value' or @attribute='value']")
     Kill all chrome instances that are opned
     Runtime.getRuntime().exex("taskkill /F /IM chromedriver.exe")



      //for windows
//put it right before setProperty
 Runtime.getRuntime().exec("taskkill /F /IM chromedriver.exe /T");
 //put like 1.5 seconds to wait
 Thread.sleep(1500);
 //set the chrome path
 System.setProperty("webdriver.chrome.driver","src/main/resources/chromedriver.exe");


 //for mac
 //put it right before setProperty
 Runtime.getRuntime().exec("killall -KILL chromedriver");
 //put like 1.5 seconds to wait
 Thread.sleep(1500);
 //set the chrome path
 System.setProperty("webdriver.chrome.driver","src/main/resources/chromedriver");


 */
public class Operators {
    public static void main(String[] args) {

    }
}
